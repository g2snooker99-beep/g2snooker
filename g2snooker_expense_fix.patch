diff --git a/app.py b/app.py
index 965baf9..9a19546 100644
--- a/app.py
+++ b/app.py
@@ -1609,7 +1609,31 @@ def manage_expenses():
         conn.execute("INSERT INTO expenses (category,amount,note,created_by,created_at) VALUES (?,?,?,?,?)",
                      (d['category'],float(d['amount']),d['note'],d['cashier'],exp_dt.isoformat()))
         conn.commit(); conn.close(); return jsonify({"status":"success"})
-    e=conn.execute("SELECT * FROM expenses ORDER BY id DESC LIMIT 30").fetchall(); conn.close()
+    # GET: กรองรายการรายจ่ายตาม "รอบกะ" เดียวกับหน้ารายงาน (ตัดยอดตาม day_cutoff_time)
+    # แทนที่จะดึง 30 รายการล่าสุดแบบไม่สนวันที่ (ซึ่งทำให้รายการค้ามข้ามกะ)
+    r1=conn.execute("SELECT setting_value FROM system_settings WHERE setting_key='day_cutoff_time'").fetchone()
+    ct=r1['setting_value'] if r1 else "06:00"
+    ch,cm=map(int,ct.split(':'))
+    now_th=datetime.now()+TZ_OFFSET
+    req_date=request.args.get('date','').strip()
+    ss_th=None
+    if req_date:
+        try:
+            base_date=datetime.strptime(req_date,'%Y-%m-%d')
+            ss_th=base_date.replace(hour=ch,minute=cm,second=0,microsecond=0)
+        except ValueError:
+            ss_th=None
+    if ss_th is None:
+        ctt_th=now_th.replace(hour=ch,minute=cm,second=0,microsecond=0)
+        if now_th<ctt_th:
+            ss_th=(now_th-timedelta(days=1)).replace(hour=ch,minute=cm,second=0,microsecond=0)
+        else:
+            ss_th=ctt_th
+    se_th=ss_th+timedelta(days=1)
+    ss=(ss_th-TZ_OFFSET).strftime('%Y-%m-%d %H:%M:%S')
+    se=(se_th-TZ_OFFSET).strftime('%Y-%m-%d %H:%M:%S')
+    e=conn.execute("SELECT * FROM expenses WHERE created_at>=? AND created_at<? ORDER BY id DESC",(ss,se)).fetchall()
+    conn.close()
     return jsonify([dict(i) for i in e])
 
 
diff --git a/templates/index.html b/templates/index.html
index 1de9e11..94a6a73 100644
--- a/templates/index.html
+++ b/templates/index.html
@@ -207,6 +207,10 @@ h6.text-muted{color:#94a3b8 !important}
       </div>
       <div class="col-md-8">
         <div class="card-panel h-100">
+          <div class="row g-2 align-items-end mb-3">
+            <div class="col-md-4"><label class="small text-muted">เลือกวันที่ (รอบกะ)</label><input type="date" id="exp-view-date" class="form-control custom-input" onchange="loadExpenses()"></div>
+            <div class="col-md-3"><button class="btn btn-outline-warning w-100 fw-bold" onclick="document.getElementById('exp-view-date').value='';loadExpenses()"><i class="fa-solid fa-rotate-left me-1"></i>กะปัจจุบัน</button></div>
+          </div>
           <table class="table table-premium text-center">
             <thead><tr><th>วันที่</th><th>เวลา</th><th>หมวดหมู่</th><th>รายละเอียด</th><th>ยอดเงิน</th><th>ผู้บันทึก</th></tr></thead>
             <tbody id="exp-table-body"></tbody>
@@ -2148,6 +2152,13 @@ function toThaiHHMM(isoStr){
   if(isNaN(d.getTime())) return '-';
   return d.toLocaleTimeString('th-TH',{hour:'2-digit',minute:'2-digit',hour12:false});
 }
+function toThaiDateStr(isoStr){
+  if(!isoStr) return '-';
+  const s = isoStr.endsWith('Z') ? isoStr : isoStr+'Z';
+  const d = new Date(s);
+  if(isNaN(d.getTime())) return '-';
+  return d.toLocaleDateString('th-TH',{day:'2-digit',month:'2-digit',year:'numeric'});
+}
 function toThaiDateTimeStr(isoStr){
   if(!isoStr) return '-';
   const s = isoStr.endsWith('Z') ? isoStr : isoStr+'Z';
@@ -2306,9 +2317,10 @@ async function deleteProduct(id, name){
 
 // ─── EXPENSES ────────────────────────────────────────────────
 async function loadExpenses(){
-  const r=await fetch('/api/expenses');const d=await r.json();
+  const dp=document.getElementById('exp-view-date')?.value||'';
+  const r=await fetch('/api/expenses'+(dp?('?date='+dp):''));const d=await r.json();
   document.getElementById('exp-table-body').innerHTML=d.map(e=>
-    `<tr><td>${toThaiHHMM(e.created_at)}</td><td>${e.category}</td><td>${e.note}</td><td class="text-danger fw-bold">${e.amount}</td><td>${e.created_by}</td></tr>`
+    `<tr><td>${toThaiDateStr(e.created_at)}</td><td>${toThaiHHMM(e.created_at)}</td><td>${e.category}</td><td>${e.note}</td><td class="text-danger fw-bold">${e.amount}</td><td>${e.created_by}</td></tr>`
   ).join('');
 }
 async function addExpense(){
